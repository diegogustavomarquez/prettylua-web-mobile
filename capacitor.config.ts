import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'prettylua-web-mobile',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
